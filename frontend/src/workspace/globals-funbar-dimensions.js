import {width, height, funBarHeight} from './globals';

const funbarY = height - funBarHeight;

const margin = funBarHeight / 7;

const functionButtonX = width * 34 / 40;
const imageButtonX = width * 37 / 40;

export default {
    funbarY,
    margin,
    functionButtonX,
    imageButtonX
}